const PI = 3.141529; 

const Math = () => {
  return ( PI );
}

const doublePI = () => {
    return ( PI * 2 );
}

const triplePI = () => {
    return (PI * 3);
}

export default Math;
export { doublePI, triplePI};
